#include <stdio.h>

int x (int n){
  if (n==1 ||n==2){
    return n;
  }
  else{
    return x(n-1)+n*x(n-2);
  }
}

int main(void) {
  int  n=4;
  
  printf("x(4) -> Resultado: %d. \n", x(n));
  
    return 0;
}






x(4) -> Resultado: 13.
